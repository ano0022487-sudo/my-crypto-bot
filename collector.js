'use strict';
const okx=require('./okx'),db=require('./db'),config=require('./config');const {normalizeTicker,normalizeCandle,normalizeTrade,normalizeOrderBook,numberOrNull,normalizeOI,normalizeFunding}=require('./normalize');const {zscore,anomalySeverity}=require('./analytics');const logger=require('./logger');
class Collector{constructor(ws){this.ws=ws;this.ids=[];this.orderbookTimer=null;this.analyticsTimer=null;this.fundingTimer=null;this.cleanupTimer=null;this.lastPersist=new Map();}
async start(){if(!db.enabled())return;this.ids=(await okx.getInstruments()).filter(x=>x.instType==='SWAP'&&x.state==='live'&&(x.quoteCcy==='USDT'||x.settleCcy==='USDT'));await db.upsertInstruments(this.ids);this.orderbookTimer=setInterval(()=>this.pollOrderBooks().catch(e=>logger.error('Orderbook collector failed',{error:e.message})),config.ORDERBOOK_POLL_MS);this.analyticsTimer=setInterval(()=>this.runAnalytics().catch(e=>logger.error('Analytics collector failed',{error:e.message})),config.ANALYTICS_POLL_MS);this.fundingTimer=setInterval(()=>this.pollFundingHistory().catch(e=>logger.warn('Funding history poll failed',{error:e.message})),10*60*1000);this.cleanupTimer=setInterval(()=>db.cleanup().catch(e=>logger.error('Cleanup failed',{error:e.message})),6*60*60*1000);await this.pollFundingHistory();await this.pollOrderBooks();}
stop(){for(const x of [this.orderbookTimer,this.analyticsTimer,this.fundingTimer,this.cleanupTimer])clearInterval(x);}
async handle(type,data){if(!db.isReady())return;try{if(type==='ticker'){const x=normalizeTicker(data),k=`ticker:${x.symbol}`;if(this.lastPersist.get(k)&&Date.now()-this.lastPersist.get(k)<5000)return;this.lastPersist.set(k,Date.now());await db.insertTicker(x);}else if(type==='candle5m'){const x=normalizeCandle(data);x.symbol=data.instId;await db.upsertCandle(x,'5m');}else if(type==='trade'){await db.insertTrade(normalizeTrade(data),data.instId);}else if(type==='open-interest'){await db.insertOI(normalizeOI(data));}else if(type==='funding-rate'){await db.insertFunding(normalizeFunding(data,data.instId));}else if(type==='liquidation'){await db.insertLiquidation(data);}}catch(e){logger.error('Collector event failed',{type,error:e.message});}}
async pollFundingHistory(){for(const id of this.ids){try{const rows=await okx.getFundingHistory(id.instId,100);for(const x of rows)await db.insertFundingHistory(id.instId,{fundingTime:numberOrNull(x.fundingTime),fundingRate:numberOrNull(x.fundingRate),realizedRate:numberOrNull(x.realizedRate),raw:x});}catch(e){logger.warn('Funding history unavailable',{symbol:id.instId,error:e.message});}}}
async pollOrderBooks(){for(const id of this.ids){try{const raw=(await okx.getOrderBook(id.instId,config.MAX_ORDER_BOOK_LEVELS))[0];if(!raw?.bids?.length||!raw?.asks?.length)continue;const bids=raw.bids.map(normalizeOrderBook),asks=raw.asks.map(normalizeOrderBook),bidVolume=bids.reduce((s,x)=>s+(x.volume||0),0),askVolume=asks.reduce((s,x)=>s+(x.volume||0),0),bestBid=bids[0]?.price??null,bestAsk=asks[0]?.price??null,total=bidVolume+askVolume;await db.insertOrderBook({symbol:id.instId,timestamp:numberOrNull(raw.ts),bestBid,bestAsk,spread:bestBid!=null&&bestAsk!=null?bestAsk-bestBid:null,bidVolume,askVolume,bidAskRatio:askVolume?bidVolume/askVolume:null,depthImbalance:total?(bidVolume-askVolume)/total:null,bids,asks});}catch(e){logger.warn('Orderbook snapshot unavailable',{symbol:id.instId,error:e.message});}}}
async runAnalytics(){if(!db.isReady())return;const rs=await db.query(`SELECT i.inst_id,t.price,t.ts,t.volume FROM instruments i LEFT JOIN LATERAL(SELECT price,volume,ts FROM ticker_snapshots WHERE inst_id=i.inst_id ORDER BY ts DESC LIMIT 1)t ON true`);for(const x of rs.rows){if(x.volume==null)continue;const base=await db.query(`SELECT volume FROM ticker_snapshots WHERE inst_id=$1 AND ts>now()-interval '24 hours' AND volume IS NOT NULL ORDER BY ts DESC LIMIT $2`,[x.inst_id,config.ANOMALY_BASELINE_POINTS]);const z=zscore(x.volume,base.rows.map(r=>r.volume));const severity=anomalySeverity(z,config.ANOMALY_THRESHOLDS);if(severity==='正常'||severity==='資料不足')continue;const id=await db.insertAnomaly({symbol:x.inst_id,timestamp:new Date(x.ts).getTime(),anomalyType:'volume',severity,measuredValue:x.volume,baseline:{mean:base.rows.length?base.rows.reduce((s,r)=>s+Number(r.volume),0)/base.rows.length:null,zscore:z,sampleSize:base.rows.length},metadata:{source:'ticker_snapshots',definition:'24h historical ticker volume baseline'}});if(id)await db.query(`INSERT INTO research_events(anomaly_id,inst_id,t0) VALUES($1,$2,to_timestamp($3/1000.0))`,[id,x.inst_id,new Date(x.ts).getTime()]);}
await db.query(`UPDATE research_events r SET t5_price=(SELECT close FROM candles c WHERE c.inst_id=r.inst_id AND c.bar='5m' AND c.ts>=r.t0+interval '5 minutes' ORDER BY c.ts LIMIT 1),t15_price=(SELECT close FROM candles c WHERE c.inst_id=r.inst_id AND c.bar='5m' AND c.ts>=r.t0+interval '15 minutes' ORDER BY c.ts LIMIT 1),t1h_price=(SELECT close FROM candles c WHERE c.inst_id=r.inst_id AND c.bar='5m' AND c.ts>=r.t0+interval '1 hour' ORDER BY c.ts LIMIT 1),t4h_price=(SELECT close FROM candles c WHERE c.inst_id=r.inst_id AND c.bar='5m' AND c.ts>=r.t0+interval '4 hours' ORDER BY c.ts LIMIT 1),t24h_price=(SELECT close FROM candles c WHERE c.inst_id=r.inst_id AND c.bar='5m' AND c.ts>=r.t0+interval '24 hours' ORDER BY c.ts LIMIT 1),completed_at=CASE WHEN now()>=r.t0+interval '24 hours' THEN now() ELSE completed_at END WHERE r.completed_at IS NULL`);
await db.query(`UPDATE research_events r SET t5_return=CASE WHEN t5_price IS NOT NULL THEN (t5_price-(SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1))/NULLIF((SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1),0)*100 END,t15_return=CASE WHEN t15_price IS NOT NULL THEN (t15_price-(SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1))/NULLIF((SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1),0)*100 END,t1h_return=CASE WHEN t1h_price IS NOT NULL THEN (t1h_price-(SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1))/NULLIF((SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1),0)*100 END,t4h_return=CASE WHEN t4h_price IS NOT NULL THEN (t4h_price-(SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1))/NULLIF((SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1),0)*100 END,t24h_return=CASE WHEN t24h_price IS NOT NULL THEN (t24h_price-(SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1))/NULLIF((SELECT price FROM ticker_snapshots t WHERE t.inst_id=r.inst_id AND t.ts<=r.t0 ORDER BY t.ts DESC LIMIT 1),0)*100 END WHERE r.completed_at IS NULL OR r.completed_at>now()-interval '24 hours'`);}
}
module.exports={Collector};
